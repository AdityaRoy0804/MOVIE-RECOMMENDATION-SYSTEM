# MOVIE-RECOMMENDATION-SYSTEM
A personalized movie recommendation system built using Collaborative Filtering (Matrix Factorization with Surprise Library). Users can log in, rate movies, and receive top movie suggestions based on their preferences. The app uses Streamlit for the UI, SQLite for user data, and a pre-trained recommendation model.
